/**
 * Globals provided by the host environment (React Native / browser).
 * The SDK stays dependency-free: we only declare what we consume.
 */

/** Defined by Metro/React Native. Absent in other environments. */
declare const __DEV__: boolean | undefined;
